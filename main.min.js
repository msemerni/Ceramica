
const langHeader=document.querySelector('.lang__header');const btnMenu=document.querySelector(".btn-menu");const menuBox=document.querySelector(".menu__body");const langHandler=(event)=>{const eventTarget=event.target;const lang=eventTarget.closest('.lang');lang.classList.toggle('show');};const chooseLang=(event)=>{const eventTarget=event.target;const text=eventTarget.innerText;const lang=eventTarget.closest('.lang');const current=lang.querySelector('.lang__current');eventTarget.innerText=current.innerText;current.innerText=text;lang.classList.remove('show');};const selectClose=(event)=>{const lang=langHeader.parentElement;const selectCurrent=langHeader.children[0];const selectArrow=langHeader.children[1];const eventTarget=event.target;if(lang.classList.contains('show')&&eventTarget!==selectCurrent&&eventTarget!==selectArrow){lang.classList.remove('show');}};if(btnMenu){btnMenu.addEventListener("click",function(event){document.body.classList.toggle('.lock-body');btnMenu.classList.toggle('menu-open');menuBox.classList.toggle('_show');});}
if(langHeader){langHeader.addEventListener('click',langHandler);const selectItems=Array.from(document.querySelectorAll('.lang__item'));selectItems.map(item=>{item.addEventListener('click',chooseLang);});document.body.addEventListener('click',selectClose);}
//# sourceMappingURL=main.min.js.map
